"use client";

import PageLayout from '../../components/PageLayout';

export default function SignupLayout({ children }) {
    return (
        <PageLayout>
            {children}
        </PageLayout>
    );
} 