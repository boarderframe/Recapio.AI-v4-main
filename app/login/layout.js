"use client";

import PageLayout from '../../components/PageLayout';

export default function LoginLayout({ children }) {
    return (
        <PageLayout>
            {children}
        </PageLayout>
    );
} 